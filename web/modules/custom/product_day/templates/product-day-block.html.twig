<h1>{{ title }}</h1>&#10{% if product %}&#10	<div class="product-day">&#10		<h2>{{ product.name.value }}</h2>&#10		<p>{{ product.summary.value }}</p>&#10		{% if product.image.entity is not null %}&#10			<img src="{{ file_url(product.image.entity.uri.value) }}"&#10					 alt="{{ product.name.value }}" width="300">&#10		{% endif %}&#10		<br/>&#10		<a href="/product/{{ product.id.value }}" class="cta-button">Learn More</a>&#10	</div>&#10{% endif %}